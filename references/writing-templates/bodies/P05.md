# P05 有证据的问题—解决

- 适用形式：短帖、Thread、文章。
- 适用条件：材料明确记录真实场景中的问题、可核实后果和处理机制。
- 必须事实：`场景`、`问题主体`、`触发动作或状态`、`可观察异常`、`一个后果`、`解决主体`、`解决动作`、`机制证据`。
- 可选事实：`会改变解决结果的条件`。
- 推进关系：`场景问题 → 可核实后果 → 解决动作 → 机制 → 证据`。
- 展开方式：后果最多一句；机制与证据相邻；必要条件紧跟对应动作。
