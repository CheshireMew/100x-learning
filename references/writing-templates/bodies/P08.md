# P08 同维度比较

- 适用形式：短帖、Thread、文章。
- 适用条件：材料同时提供至少两个对象和可统一衡量的维度。
- 必须事实：`对象 A`、`对象 B`、`共同任务`、`一至四个共同维度`、`双方事实`。
- 可选事实：`用户确实需要选择时的选择条件`。
- 推进关系：每个维度都写 `标准 → A → B → 本维度结论`，再进入下一维度。
- 展开方式：一段只比较一个维度；双方使用同一时间和口径。
