# P07 分层机制拆解

- 适用形式：Thread、文章；简单机制可用于短帖。
- 适用条件：系统或现象由二至六个彼此依赖的机制层构成。
- 必须事实：`总输入`、`每层作用`、`每层变化`、`每层输出`、`最终结果`。
- 可选事实：`已知失效条件`。
- 推进关系：`总问题 → 第一层 → 第二层 → … → 最终输出`。
- 每层关系：`本层输入 → 发生什么变化 → 输出给下一层什么`。
