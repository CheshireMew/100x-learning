# P10 可复现步骤

- 适用形式：短帖、Thread、教程文章。
- 适用条件：材料有顺序明确、可重复执行的操作链。
- 必须事实：`起点`、`前提`、`三至八步动作`、`每步可见结果`、`最终验收`。
- 固定顺序：`结果目标 → 前提 → 步骤一…N → 最终验收`。
- 每步骨架：`动作/命令 → 预期可见结果`；失败与恢复交给 `S01`。
- 禁用：未验证命令；步骤之间缺必要前提；把功能介绍伪装成教程。
