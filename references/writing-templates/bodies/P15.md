# P15 实验或项目迭代复盘

- 适用形式：Thread、文章、Newsletter。
- 适用条件：材料来自真实实施，记录目标、选择、问题、改动和结果。
- 必须事实：`原目标`、`初始选择及原因`、`实施动作`、`问题`、`改动`、`新结果`、`重做选择`。
- 固定顺序：`目标 → 初始选择 → 实施 → 问题 → 修改 → 结果 → 当前状态 → 重做选择`。
- 每轮骨架：`本轮假设 → 改了什么 → 测到什么 → 保留/推翻什么 → 下一轮`。
- 禁用：虚构失败；多个变量混在一起却声称找到原因；抽象经验教训代替结果。
