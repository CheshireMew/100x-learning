# P19 逐级升级

- 适用形式：短帖、Thread、文章。
- 适用条件：材料存在同一问题或行动在至少三个阶段中的真实升级。
- 必须事实：`起始状态`、`阶段一`、`阶段二`、`阶段三或结果`、`每次升级触发条件`。
- 固定顺序：`起始状态 → 第一次升级 → 新问题 → 第二次升级 → 最终状态`。
- 逐段骨架：每次升级都写触发条件和新增动作，不能只加更强形容词。
- 禁用：普通列表被写成递进；阶段之间没有新约束；为了戏剧性不断加码。
