# P16 多转折长时间线

- 适用形式：长 Thread、文章。
- 适用条件：材料有按时间排序的事件、至少两个转折、决定性动作、结果和后续影响。
- 必须事实：`现场起点`、`必要背景`、`转折一`、`转折二`、`决定性动作`、`结果`、`后续影响`。
- 固定顺序：`现场 → 背景 → 转折一 → 冲突升级 → 转折二/决定动作 → 结果 → 后续影响`。
- 每节骨架：使用 `S05`，一次只写一个转折。
- 禁用：另开无关技术主线；补人物内心或虚构场景；打乱时间只为悬念。
