# P11 重复单元清单

- 适用形式：短帖、Thread、工具目录文章。
- 适用条件：同一任务下有二至十五个独立对象，可以使用同一字段说明。
- 必须事实：`共同任务`、`条目数量`、`共同标准`、`每项名称`、`每项用途`、`每项区别事实`。
- 固定顺序：重复 `名称 → 用途 → 一项具体特点 → 限制或适用条件`。
- 逐段骨架：承诺几项就写几项；条目之间不写总结性过渡。
- 禁用：凑数；每项使用不同广告句；无依据排名；末尾重新抄一遍清单。
