---
name: 100x-learning
description: 帮助用户从材料、主题或实际问题中提炼知识，讲清概念，根据真实反馈连续学习，筛选和维护持续选题，复盘已发布内容，并直接写成短帖、GitHub 项目介绍或文章；也为产品、项目、功能、活动、资源和作品设计或写作内容型宣发，并初始化、检查、接入和维护不随 Skill 发布的本机私人知识库。Use when the user asks to 划重点、研究主题、解释概念、系统学习或继续下一篇、判断资料和学习方法、读取视频或社交来源、分轮处理长材料、建立长期内容方向、筛选或维护选题、用真实指标和评论复盘发布内容、筛选分享候选、把研究结果写成内容，初始化或检查私人知识库，审计或逐批接入大量本地材料，沉淀知识、内容案例或钩子，或明确要求宣发、推广、上线宣传、活动招募、预热、发布期以及引导试用或报名；媒体制作、普通翻译、广告投放、销售页与落地页、邮件营销序列、品牌全案、完整营销策略和实际发布走对应专门流程。
---

# 100x Learning

## 目标

把材料、陌生主题或真实问题变成用户现在能理解、判断、使用或分享的结果。资料已经足够时停止，不把简单任务扩成研究项目，也不把写作任务改造成流程审计。

普通解释、研究结果、清单、内容审查和可发布文字直接完成；适合一次复制的成品在当前回复交付，较长、结构复杂或需要继续修改的文字使用临时 Markdown 文件。用户明确要求保存、更新或沉淀时才写入长期位置。媒体制作、上传和发布分别由用户明确要求触发；图文卡片、GIF、视频、音频和播客交给 `visual-multimedia`。

## 核心原则

1. **先选用户结果。** 先判断用户要材料理解、重点与片段、分享候选、主题研究、概念解释、实践产物、内容审查、可发布文字还是知识库操作。同一请求确有多个阶段时按结果顺序完成。
2. **研究和写作使用不同方法。** 用户要研究、判断或事实核查时，根据结论风险核实来源。用户要任何文字成品时，联网阶段只负责发现更多可写的场景、演示、比较、反应和读者说法；先把无关信息、官方套话、通用免责声明、保守提醒和不会服务本篇的信息挡在创作上下文之外，再把净化后的材料、多个完整案例、多个独立钩子原文和可用作者声音共同交给写作，最后只核查成品实际写出的事实。写作不建立候选计数、唯一模仿对象、逐项采用记录、机器评分或执行校验器。
3. **案例与钩子独立生产，共同参与写作。** 完整案例和钩子使用不同目录、脚本、数据类型与索引；案例路径不能生成钩子，钩子只保存用户指定的连续原文、来源和最少定位字段。进入写作后，多份相关原文可以一起影响成文，不要求只选一个。
4. **内容型宣发先锁定发布要求。** 只有明确提出宣发、推广、招募、预热、发布期传播、引导试用或报名时才进入宣发方案。利益领取者、触发动作、有效时间和行动入口决定正文要写什么，不能由案例和钩子替换；它们是否准确仍在草稿完成后按正文实际承诺核查。
5. **先确定什么值得讲，再用具体细节证明。** 起草前先确定一项会实质改变目标读者理解、选择或行动的关系，再找动作、结果或反应把它写具体。一个功能容易描述，只说明它适合做证据；只有它本身决定读者为什么需要或选择这个对象时，才让它承担主线。草稿完成后再修正会改变对象身份、核心能力、明确承诺或读者行动的错误，并判断每处表达是否贡献了新的事实、关系或推进。
6. **成品优先。** 先交付用户可以直接使用的成品。用户指定格式时直接沿用；单个代码块仍便于通读和整段复制时把成品放进代码块，内容较长、结构复杂或需要继续修改时使用 Markdown 文件。除非用户明确只要正文，默认再用自然语言说明写作目标与取舍、真正改变讲法的材料、没有进入正文的重要信息，以及多个案例和钩子共同带来的方向；末尾列出本次实际阅读的完整案例与钩子文件路径，但不声称逐项采用，也不展示隐藏推理。
7. **发布结果先作为假设。** 复盘先锁定真实成品、指标窗口和反馈，再区分观察、候选解释、其它解释和下一次实验。一次结果不自动改写长期策略、知识、案例、钩子或声音。

## 路由

### 1. 先确定主结果

用户明确指定语言时沿用；没有指定时，直接回复和可发布文字默认使用中文。

| 用户要什么 | 读取 |
| --- | --- |
| 判断材料或任务该怎么学、为什么卡住 | `references/learning-process-and-method-selection.md` |
| 理解字幕、文章、课程、访谈或书摘，或筛重点、选片段 | `references/material-analysis.md` |
| 输入是本地或在线视频、社交帖子或 Thread | 先读 `references/source-ingestion.md`，建立内容真源后再进入用户所需的材料理解、研究、筛选或写作路线 |
| 从材料中筛选值得分享的内容 | 长材料先读 `references/material-analysis.md`，再读 `references/shareable-content-selection.md` |
| 主动研究主题、比较来源或核查重要事实 | 先读 `references/research-context-reuse.md`，私人库已配置时再读 `references/private-knowledge-library.md` 和 `references/knowledge-base-workflow.md`，随后读 `references/research-led-learning.md` |
| 解释、比较或连接概念 | `references/concept-deconstruction.md` |
| 系统学习一个主题，或根据实际反馈继续下一篇 | `references/continuous-learning.md`；需要跨任务保存时再读 `references/private-knowledge-library.md` 和 `references/knowledge-base-workflow.md` |
| 把知识用于模拟、分析、工具、方案或判断 | `references/practice-led-learning.md` |
| 建立长期内容方向、选择选题或维护持续选题项目 | `references/content-strategy-and-topic-selection.md`；需要读写长期状态时再读 `references/private-knowledge-library.md` 和 `references/knowledge-base-workflow.md` |
| 用真实指标、评论或后续行动复盘已发布内容 | 读 `references/published-content-review.md` 和 `references/practice-led-learning.md`；需要定位或保存成品时再读 `references/private-knowledge-library.md`、`references/personal-writing-memory.md` 和 `references/knowledge-base-workflow.md` |
| 只审查内容、事实、AI 味或字词，不要求改稿 | `references/content-audit.md`；AI 味审查另读 `references/natural-writing.md` 的“成稿后” |
| 只要求设计宣发方案，或提出推广目标但没有指定文字成品 | 进入宣发方案 |
| 起草、改写、扩写、压缩或重组可发布文字 | 先选择成品身份，再判断是否叠加宣发要求 |
| 初始化或接入私人知识库 | `references/private-knowledge-library.md` |
| 检查私人知识库的结构、复核日期、冲突、未消化来源或关联 | 先读 `references/private-knowledge-library.md`，再读 `references/knowledge-base-health.md`；用户确认修复后再读 `references/knowledge-base-workflow.md` |
| 检索、保存、更新或从材料沉淀知识、内容案例与钩子 | 先读 `references/private-knowledge-library.md`，取得根目录后读 `references/knowledge-base-workflow.md`；涉及完整案例时再读 `references/content-case-library.md`，涉及钩子时再读 `references/hook-library.md` |
| 审计并逐批把大量本地材料接入私人知识库 | 先读 `references/private-knowledge-library.md` 和 `references/knowledge-base-workflow.md`，再读 `references/bulk-knowledge-ingestion.md`；需要跨轮恢复时另读 `references/durable-learning-projects.md` |

用户只点名本 Skill 或只附材料而没有指定产物时，默认先讲清材料本身。用户只要研究、候选、审查或大纲时，在该结果完成后停止，不自动继续写成帖子。当前请求始终覆盖长期内容策略；一次性选题在回复中完成，只有明确的持续系列才维护项目状态。连续学习每轮只完成当前有用的一课，并根据真实反馈决定下一轮；单份长材料确实不能在当前任务可靠完成时，先读 `references/durable-learning-projects.md` 再分轮推进。大批量接入先通过代表性样本证明知识库能被真实任务消费，再经用户授权逐批扩大。

### 2. 选择写作动作和成品身份

写作动作只选用户实际要求的一种：大纲、起草、改写、扩写、压缩、重组或审查后修改。再选择成品形态；“教程、方法、介绍、观点”等表达任务只说明内容讲什么，不决定长短。用户说“短内容”“简单写一下”或明显准备直接发布一小段文字时，默认选择短帖；只有用户明确要文章、Newsletter、长文、完整教程，或正在修改一篇已有长文时才选择文章。

| 成品 | 方法 |
| --- | --- |
| 独立帖、引用帖、资源分享、产品帖、Thread、通用短帖 | `references/content-writing.md` |
| 单个 GitHub 项目介绍 | `references/github-project-short-content.md`，成文时再用 `references/content-writing.md` |
| GitHub 项目清单 | `references/github-project-list.md`，成文时再用 `references/content-writing.md` |
| 文章或 Newsletter | `references/article-from-practice.md`，成文时再用 `references/content-writing.md` |

表达任务按用户目的选择：正面介绍或推荐、评测比较或锐评、解释教程或方法、观点或经历、清单选择或排名、新闻发布或近期变化。

### 3. 判断是否叠加内容型宣发

成品身份确定后，只有用户明确要求宣发、推广、招募、预热、发布期传播、引导试用或报名，或者文字必须承接利益、时点和转化动作时，才读取 `references/promotional-content-writing.md` 和 `references/prewriting-research.md`。普通介绍、分享、推荐、产品帖，以及“病毒式传播”“更有传播力”“爆款”等效果要求继续使用已经选定的成品身份；传播效果由 `references/content-writing.md` 实现，不触发宣发方案。

用户或甲方给出的数据、定位、必写内容、禁用叙事和成品数量是当前写作要求，不在起草前自动展开外部核查。材料不足时，联网只补充能帮助选材和成文的信息；用户同时要求研究、判断或事实核查时，先完成那个独立结果，再把真正适合公开表达的部分交给写作。

宣发要求必须锁定对象与时点、成品数量、主要读者、希望发生的变化、读者利益、发布者或合作方激励、领取者与触发动作、开始与结束时间、核心信息、可写材料、内容分工和真实入口。百分比不知道给谁，或“限时两周”没有起算锚点且会改变核心信息与 CTA 时，只问这个关键缺口并停止。

用户只要求方案，或提出宽泛推广目标却没有指定文字成品时，交付可确认方案并停止；用户已经要求起草具体宣发文字、确认了方案或只修改既有宣发稿时，直接带着同一份写作要求进入写作。单篇请求只交付一个成品；多个卖点和角度只是选材，不自动拆成多篇。

## 写作流程

### 1. 锁定当前输入

确认对象、成品数量、动作、成品形态、表达任务、目标读者、读者准备怎样使用成品、语言和用户明确格式。用户没有指定读者，且内容面向公开传播又没有专业受众线索时，默认写给普通人。先按用户要求把输入区分为正在修改的正文、必须逐字保留的内容、为新稿提供事实与场景的材料，以及明确要求仿写或沿用的创作参考。只有正在修改的正文和用户明确要求沿用的结构继续约束成品组织；为新稿提供内容的完整文案即使已经成篇，也只提取可用的动作、结果、比较和说法，不默认沿用段落顺序。

用户说“根据、基于、结合某份材料写”时，默认把材料作为新稿的事实与场景来源，不把它解释成忠实摘要合同。材料约束直接引语、数字、人物身份、具体事件和不能被改变的核心关系；标题、角度、组织、过渡、基于可观察行为的作者判断、读者关系和结尾由当前成品重新创造。只有用户明确要求摘要、忠实转述、逐句改写、不作推断或不得超出材料原意时，才把表达限制在相应范围内。这一判断同时适用于短帖、Thread、项目介绍、文章和 Newsletter。

同一对话中，用户评价完一个同类型成品后立即更换对象继续写，把明确认可或否定的写作维度作为本轮局部要求，不沿用上一对象的事实、原句和一次性结构。用户只纠正一个维度时，只改变该维度；同一连续写作中已经明确认可的方向和尚未撤回的写作要求继续有效。其它任务的讲法、开头和反馈只有用户明确要求时才带入。

### 2. 发现并净化有现实感的写作材料

读取 `references/prewriting-research.md`。普通新写、扩写或实质重组围绕当前目标寻找更多可写材料；只做字词、格式、等义压缩，或用户明确只用给定材料时不联网。不联网只限制信息来源，不会把创作自动降成摘要或逐句释义。

GitHub 项目先快速打开用户给出的仓库或页面，认清对象并看到主要演示；再从官方材料、媒体报道、KOL 或创作者演示、真实用户体验、Issue、Discussion 和社区讨论中寻找不同的场景、结果、比较、反应和自然说法。搜索是为了扩充创作可能性，不在写作前逐项证明 README。打开实际来源阅读全文或相关上下文，不用搜索摘要代替。

搜索完成后只把真正可能服务本篇的动作、结果、演示、比较、反应、说法和少量必要解释放进创作上下文，同时保留对应链接供成稿后核查。官方免责声明、通用风险提醒、保守陈词、无关功能、边缘限制以及只供备查的技术信息不进入写作材料。没有找到有用的外部讨论时直接沿用现有材料，新来源开始重复已有角度时停止，不按固定来源数凑材料。

正文可能引导读者实际使用项目、资源或产品时，再从官方材料带回项目主页、仓库、安装指南、原样候选命令及其实际完成范围和前置步骤。这里只准备候选，不因一段 Shell 文本刚好写在一行就提前用它替代项目入口。只有用户明确要求提示词，或正文的主要下一步本来就是把一个明确任务交给 AI 时，才带回完成该提示词所需的目标、必要边界和验收依据。AI 无法完成核心动作时不把它包装成可代办任务。

### 3. 确定作者位置与内容职责

需要延续本人写法时，先读取 `references/private-knowledge-library.md` 并取得私人库根目录，再读取 `references/personal-writing-memory.md`；声音检索与内容查重分开使用。私人库尚未初始化时，使用当前原话和有效样稿继续，不临时建立声音。再读取 `references/natural-writing.md` 的“写作前”，确定作者为什么讲、读者在意什么、这篇最值得讲什么、希望读者发生什么变化、必须写哪些内容以及停在哪里。此时不固定第一句和段落顺序。

### 4. 读取专项模板与多份创作参考

先读取已经选定的专项模板，再读取 `references/private-knowledge-library.md` 取得私人库根目录。完整案例通过 `references/content-case-library.md` 和 `<私人知识库>/20-Sources/Content Cases/内容案例索引.md` 按完整成品的写作任务定位；独立钩子通过 `references/hook-library.md` 和 `<私人知识库>/20-Sources/Hook Library/开头钩子索引.md` 按开头手法定位，不能用题材标签代替手法。根据当前对象和读者浏览相关原文，范围仍然过大时直接搜索标题、正文与隐藏索引。私人库尚未初始化或参考库为空时，使用当前材料和作者判断继续。程序不替模型评分、排序或预先筛掉创作参考；用户要求批量整理钩子时，由模型比较相似候选，数据缺失不自动淘汰，筛选指标和理由不进入活动钩子或索引。

打开多份方向不同且真正相关的完整案例，以及多份独立钩子的原始开头与紧接内容。参考数量由差异、质量和上下文容量决定，不固定三条、六条或必须试写几篇；已经有足够启发时停止，找不到更贴合的参考也可以根据当前可写材料继续写。索引和标签只帮助定位，不代替原文，不限定模型只能学习哪一点。

内容型宣发只打开 `index_roles` 包含 `promotion` 且利益领取者与当前任务一致的案例，避免把发布者返佣案例当成读者优惠。只交付宣发方案时不读取写作案例与钩子。

### 5. 综合现实材料与创作参考，直接成文

使用 `references/content-writing.md`。把净化后的可写材料、外部真实声音、作者位置、专项模板、多个完整案例和多个独立钩子原文共同作为写作输入，由本次对象和读者关系决定开头、推进、信息顺序与收束。可以同时学习多份参考，也可以只受其中一部分影响；不挑唯一案例，不登记逐项采用或未采用，不做反事实证明，不让参考增加成品数量或用户没要的内容。

用户希望内容更有传播力时，先从材料中确认一项会改变读者理解、选择或行动的核心关系，再用动作、结果或判断把它写具体。对项目介绍，先回答项目让读者获得了什么值得在意的能力、结果或控制，再决定哪些功能只是证明；第一段建立的期待必须由正文兑现。不能指出这项关系及其材料时，继续寻找或收窄主张，暂不写开头。

字幕、访谈和会议记录通常是按时间切开的表达碎片。写作者先恢复谁在回应什么、前后说法如何变化以及真正推进的发现，再按成品需要重组；作者判断必须能回到材料中的动作或关系。短内容压缩到最值得复述的一条关系，长内容提供理解这条关系所需的展开，但两者都不把材料顺序当作正文结构。

面向普通读者时，正文围绕熟悉的问题、能看到的变化和下一步行动展开。产物、数字和演示只用于把变化说具体；工作流、文件格式和内部术语只有在读者必须理解它们才能判断或行动时才进入正文，必要术语第一次出现时立即用日常语言解释。案例中的第一人称、接触动作和个人经历不能转写成本次作者经历；确有当前作者提供的亲历、测试结果或效果时，才作为可信度依据写入并在成稿后核查。

正文包含读者要完成的具体任务时，选择最省力且能够真正完成的行动路径。提示词不是默认组成：只有用户明确要求，或者正文的主要下一步本来就是把一个明确任务交给具备目标环境操作能力的 AI，而且提示词比项目入口更直接地帮助读者完成任务时，才给出一至三句可以直接复制的提示词，写清目标、必要上下文或边界和完成后的验证结果。只是介绍、推荐、制造兴趣或提供项目入口时不加提示词；需要读者自己完成时再写真正必要的手动步骤。

面向一个可访问的项目、资源或产品时，行动入口必须服务本篇真正希望读者完成的下一步。普通介绍、推荐或分享默认把项目主页、仓库或其它有用页面作为入口；安装、运行或教程内容才从候选中选择能够直接完成主要动作的指令或安装页面。具体选择和呈现统一由 `references/content-writing.md` 执行，专项模板和成稿清理不再按命令外形重新选择。项目清单保留每项自己的行动入口；只有整份清单共同服务一个明确任务时才给一段共享提示词。用户指定格式时按用户格式执行。

### 6. 回读成品

应用用户明确格式后，读取 `references/natural-writing.md` 的“成稿后”。只对正文实际出现的对象身份、核心能力、数字、日期、利益归属、时间、AI 提示词可执行性、行动入口和其它会改变读者理解或行动的说法做事实核查；修正或删除实质错误，不补入来源里的免责声明、限制清单和稳妥提醒。表达近似只有在材料足以支持、不会改变核心事实或行动，而且确实让关系更清楚时才保留。随后检查每句话是否贡献事实、关系或推进，并确认第一段建立的期待已由正文兑现。

### 7. 直接交付

先交付正文或大纲。用户明确格式优先；成品放进一个代码块仍便于通读和整段复制时使用代码块，内容较长、结构复杂、包含多段代码或需要继续修改时写入 Markdown 文件并返回链接。用户只想预览或讨论草稿时直接回复，不为了“文章”这个名称自动写文件。除非用户明确只要正文，再附一段足够详细的写作说明，用完整段落说明写作目标与取舍、真正改变选材和讲法的材料、没有进入正文的重要技术信息或限制，以及多个案例与钩子共同带来的方向；最后以“本次创作参考（实际阅读）”列出本次打开的完整案例和钩子文件路径。说明不声称逐项采用，不输出分数、候选状态、JSON、隐藏推理或检查日志。

## 知识库与持久化

私人知识库是独立于 Skill 源码的用户运行库。初始化、接入、读取或写入前先读 `references/private-knowledge-library.md`，由 `scripts/private_library.py` 返回本次唯一根目录；知识、案例、钩子和写作记忆不再根据当前仓库或客户端目录猜测位置。

普通解释、预览稿和适合一次复制的短成品留在当前回复；较长、结构复杂或需要继续修改的文字成品可以使用临时 Markdown。用户明确要求保存、更新或沉淀时，取得私人库根目录后按 `references/knowledge-base-workflow.md` 找到唯一长期位置。知识库已有同主题内容时更新真源，确实是独立主题时才新建。私人库已经配置 Marktree CLI 时，持久化正文、案例和索引统一经过 `scripts/marktree_integration.py`，让桌面端、Agent 与可选 Git 读取同一份真实文件和精确变更；知识语义仍只由本 Skill 与 `Home.md` 决定。

用户明确要求根据给定材料沉淀时，先完成材料理解，再按材料实际角色组合写入：需要长期保留的原始材料进入 `20-Sources`；稳定概念、机制、结论和边界合并到 `10-Knowledge` 的唯一主题；材料本身是完整成品且具有创作参考价值时才进入完整案例。只有用户明确要求把一段原始开头保存为钩子时，才由独立入口保存这段连续原文及其紧接内容；案例导入不会派生钩子。写后重新读取真源并运行各自的索引验证，告诉用户实际更新的文件。

发布历史和声音证据使用 `scripts/writing_memory.py` 的两个入口：`voice` 只读明确允许用于声音学习的同形态作品，`novelty` 只判断主题是否重复。用户确认、保存或更新最终作品后，按 `references/personal-writing-memory.md` 更新索引。

长期内容方向与声音分别使用 `60-Systems/Writing/content-strategy.md` 和 `60-Systems/Writing/style-guide/voice.md` 作为唯一真源。持续选题只在真实内容项目的 `30-Projects/Content/<系列>/topic-portfolio.md` 保存决策状态；已发布内容复盘链接最终正文和真实反馈，不复制正文。一次结果保持为假设，只有重复证据、直接行为证据或用户确认支持的认识才进入对应长期真源，发布表现本身不改写声音。

用户明确要求保存或更新完整案例时，先读取 `references/content-case-library.md`，使用 `scripts/content_case_library.py add-case` 写入完整短内容或文章案例，再运行该脚本的 `build-index` 和 `validate`。用户明确要求保存或更新钩子时，先读取 `references/hook-library.md`，使用 `scripts/hook_library.py add-hook` 写入独立钩子库，再运行该脚本的 `build-index` 和 `validate`。两个入口没有互相转换或引用的参数。

本地 SRT、VTT、带时间戳 TXT 或断行严重的字幕需要规范化时，使用 `scripts/normalize_subtitles.py`，保留原文件和真实时间位置。

个人写作记忆只提供经过确认的声音证据与发布历史；内容案例库只提供创作参考，两者不能替代当前对象的核心能力和明确承诺。普通翻译、转写、格式清洗和普通计划走对应专门流程；广告投放、销售页与落地页、邮件营销序列、品牌全案、完整营销策略和实际发布不属于本 Skill 的文字写作结果。

## 交付前检查

- 第一段已经回答用户真正的问题；资料足够时已经停止。
- 写作前的搜索补充了可写材料，没有提前扩张成逐项事实调查；只供备查的信息没有进入创作上下文。
- 成品数量、语言、长度、链接位置和其它明确格式符合请求。
- 内容型宣发的读者利益、发布者或合作方激励、领取者、触发动作、时间和入口保持对应。
- 完整案例与钩子使用独立脚本、目录、数据类型和索引，生产时不能互相派生或引用；写作时多份相关原文可以共同进入上下文。
- 成稿由净化后的材料、真实外部语境和作者声音驱动，没有唯一参考、逐项采用记录、固定候选数、执行 JSON 或写作校验器。
- 成品形态没有被“教程、方法、介绍”等表达任务误判；“短内容、简单写一下”没有自动扩成文章。
- 面向普通读者的正文不要求读者先理解技术实现；读者能直接说清发生了什么、与自己有什么关系以及下一步怎么做。
- AI 提示词只在用户明确要求，或它本来就是正文的主要下一步且确实帮助读者完成任务时出现；没有因为 AI 可以代办而机械加入，手动步骤也只在真正需要时展开。
- 行动入口由正文目的和读者下一步决定；普通介绍、推荐或分享没有被候选命令抢走项目入口，安装或教程中的直接指令能够完整完成所承诺的主要动作并得到自然说明。项目清单仍保留每项入口。
- 第一人称具体经历只来自当前作者或当前材料中的明确事实，没有从案例移植成作者经历。
- “根据材料写”没有被误判为忠实摘要；短内容与长内容都能在事实锚点之外形成自然场景、作者判断和新的读者价值。
- 成稿只核查实际写出的重要事实，表达性小误差没有被机械修平，事实核查也没有向正文补入免责声明。
- 交付载体服从实际使用：易于一次复制的成品使用代码块，较长、复杂或需继续修改的成品使用 Markdown，预览和讨论不自动写文件。
- 除非用户明确只要正文，写作说明已经交代目标、取舍和材料影响，并列出本次实际阅读的完整案例与钩子路径，没有把它变成采用审计。
- 视频读取深度由用户结果决定；关键画面与同一时间范围的转写对应，社交来源的缺失状态没有被写成零或原文事实。
- 可恢复项目只登记真实来源和真实输出；恢复与完成前已经重新核对来源、输出和汇总成品。
- 长期内容策略没有覆盖当前要求；一次性选题没有被扩成项目，持续项目只保存状态和真源链接，没有评分或固定数量。
- 发布复盘已经锁定真实成品和指标窗口，观察、候选解释、其它解释与下一次实验分开；单次结果没有被自动提升为长期规则或声音证据。
- 私人知识库健康检查默认只读；语义问题经过实际文档和来源复核，修复只有在用户确认后发生。
