---
name: 100x-learning
description: 帮助用户读懂材料、研究主题、解释概念、把知识用于真实问题，并直接写成短帖、GitHub 项目介绍或文章；也能接入视频和社交来源，筛选分享内容，根据发布反馈继续选题和改进内容，以及初始化、检查和维护本机私人知识库、内容案例、钩子与写作记忆。适用于划重点、系统学习、分轮处理长材料、内容审查、宣发写作和知识沉淀；媒体制作、普通翻译、广告投放、销售页、邮件营销、品牌全案、完整营销策略和实际发布使用对应的专门流程。
---

# 100x Learning

## 目标

把材料、陌生主题或真实问题变成用户现在能理解、判断、使用或分享的结果。资料已经足够时停止，不把简单任务扩成研究项目，也不把写作任务改造成流程审计。

普通解释、研究结果、清单、内容审查和可发布文字直接完成；适合一次复制的成品在当前回复交付，较长、结构复杂或需要继续修改的文字使用临时 Markdown 文件。用户明确要求保存、更新或沉淀时才写入长期位置。媒体制作、上传和发布分别由用户明确要求触发；图文卡片、GIF、视频、音频和播客交给 `visual-multimedia`。

## 核心原则

1. **先选用户结果。** 先判断用户要材料理解、重点与片段、分享候选、主题研究、概念解释、实践产物、内容审查、可发布文字还是知识库操作。同一请求确有多个阶段时按结果顺序完成。
2. **研究和写作使用不同方法。** 用户要研究、判断或事实核查时，根据结论风险核实来源。用户要文字成品时，联网寻找能够增加创作可能性的场景、演示、比较、反应和读者说法；网络来源只补充当前对象的可写材料，不改变内容形式，也不自动加载文章案例或作者声音。取舍后保留让内容成立的完整事实关系、具体细节和必要上下文，再与用户要求、同类完整案例、独立钩子原文和按需取得的作者声音共同成文。
3. **案例与钩子分开保存，共同参与写作。** 完整案例和钩子使用不同目录、脚本、数据类型与索引；案例路径不能生成钩子，钩子只保存用户指定的连续原文、来源和最少定位字段。进入写作后，多份相关原文可以一起影响成文，不要求只选一个。
4. **发布事实只叠加，不接管写作。** 宣发、推广、招募、预热、发布期传播、引导试用或报名仍按用户选择的成品和普通写作流程完成。正文确实包含福利、时限、行动入口或披露要求时，才补入相应事实；它们不决定语气、开头、结构和产品怎样出现。
5. **让材料扩大创作空间。** 搜索保留真实细节、上下文和关系，完整案例帮助发现内容可能性与材料缺口，钩子帮助放大已经成立的内容。它们不替模型预先指定唯一主线、固定开头、段落数量或信息顺序；模型先根据对象、读者、完整材料和成品用途形成这篇实际要讲的内容，再决定怎样表达。
6. **写作结果完整可见。** 每次写作交付都依次展示任务合同、实际传给写作 AI 的任务与对象材料、写作 AI 的完整成品，三部分分别使用代码块。本次实际用于写作的完整案例与钩子只在代码块外的“本次创作参考”中列出可点击文件，不把参考全文或路径混入三个代码块。
7. **发布结果先作为假设。** 复盘先锁定真实成品、指标窗口和反馈，再区分观察、候选解释、其它解释和下一次实验。一次结果不自动改写长期策略、知识、案例、钩子或声音。

## 路由

### 1. 先确定主结果

用户明确指定语言时沿用；没有指定时，直接回复和可发布文字默认使用中文。

| 用户要什么 | 读取 |
| --- | --- |
| 判断材料或任务该怎么学、为什么卡住 | `references/learning-process-and-method-selection.md` |
| 理解字幕、文章、课程、访谈或书摘，或筛重点、选片段 | `references/material-analysis.md` |
| 从材料中筛选值得分享的内容 | 长材料先读 `references/material-analysis.md`，再读 `references/shareable-content-selection.md` |
| 主动研究主题、比较来源或核查重要事实 | 先读 `references/research-context-reuse.md`，私人库已配置时再读 `references/private-knowledge-library.md` 和 `references/knowledge-base-workflow.md`，随后读 `references/research-led-learning.md` |
| 解释、比较或连接概念 | `references/concept-deconstruction.md` |
| 系统学习一个主题，或根据实际反馈继续下一篇 | `references/continuous-learning.md`；需要跨任务保存时再读 `references/private-knowledge-library.md` 和 `references/knowledge-base-workflow.md` |
| 把知识用于模拟、分析、工具、方案或判断 | `references/practice-led-learning.md` |
| 建立长期内容方向、选择选题或维护持续选题项目 | `references/content-strategy-and-topic-selection.md`；需要读写长期状态时再读 `references/private-knowledge-library.md` 和 `references/knowledge-base-workflow.md` |
| 用真实指标、评论或后续行动复盘已发布内容 | 读 `references/published-content-review.md` 和 `references/practice-led-learning.md`；需要定位或保存成品时再读 `references/private-knowledge-library.md`、`references/personal-writing-memory.md` 和 `references/knowledge-base-workflow.md` |
| 只审查内容、事实、AI 味或字词，不要求改稿 | `references/content-audit.md`；AI 味审查另读 `references/natural-writing.md`；正文含福利、时限、行动入口或披露时再读 `references/publication-requirements.md` |
| 只要求内容方案、发布安排或传播思路，没有要求具体文字 | 把方案作为本次成品；需要具体开头、选题或表达方向时仍可读取案例与钩子，交付方案后停止 |
| 起草、改写、扩写、压缩或重组可发布文字 | 先选择内容形式，再按需叠加发布事实 |
| 初始化或接入私人知识库 | `references/private-knowledge-library.md` |
| 检查私人知识库的结构、复核日期、冲突、未消化来源或关联 | 先读 `references/private-knowledge-library.md`，再读 `references/knowledge-base-health.md`；用户确认修复后再读 `references/knowledge-base-workflow.md` |
| 检索、保存、更新或从材料沉淀知识、内容案例与钩子 | 先读 `references/private-knowledge-library.md`，取得根目录后读 `references/knowledge-base-workflow.md`；涉及完整案例时再读 `references/content-case-library.md`，涉及钩子时再读 `references/hook-library.md` |
| 审计并逐批把大量本地材料接入私人知识库 | 先读 `references/private-knowledge-library.md` 和 `references/knowledge-base-workflow.md`，再读 `references/bulk-knowledge-ingestion.md`；需要跨轮恢复时另读 `references/durable-learning-projects.md`；材料确实要进入完整案例或钩子时，再分别读取 `references/content-case-library.md` 或 `references/hook-library.md` |

用户只点名本 Skill 或只附材料而没有指定产物时，默认先讲清材料本身。用户只要研究、候选、审查或大纲时，在该结果完成后停止，不自动继续写成帖子。当前请求始终覆盖长期内容策略；一次性选题在回复中完成，只有明确的持续系列才维护项目状态。连续学习每轮只完成当前有用的一课，并根据真实反馈决定下一轮；单份长材料确实不能在当前任务可靠完成时，先读 `references/durable-learning-projects.md` 再分轮推进。大批量接入先通过代表性样本证明知识库能被后续任务实际读取和使用，再经用户授权逐批扩大。

### 2. 再按输入和持续方式补充条件

用户给的是本地视频、在线视频、社交帖子或 Thread 时，先读 `references/source-ingestion.md`，取得可靠的正文、时间位置、关键画面和缺失状态，再进入已经选定的材料理解、研究、筛选或写作路线。来源接入只准备材料，不改变主结果。

需要跨任务保存、恢复或维护长期状态时，按主结果中已经列出的私人知识库和可恢复项目说明执行；普通回复和一次能够完成的材料不因此增加持久化流程。

### 3. 选择写作动作和内容形式

写作动作只选用户实际要求的一种：大纲、起草、改写、扩写、压缩、重组或审查后修改。再确定要写成哪种内容；“教程、方法、介绍、观点”等只说明内容讲什么，不决定长短。用户说“短内容”“简单写一下”或明显准备直接发布一小段文字时，默认选择短帖；只有用户明确要文章、Newsletter、长文、完整教程，或正在修改一篇已有长文时才选择文章。

| 成品 | 方法 |
| --- | --- |
| 独立帖、引用帖、资源分享、产品帖、Thread、通用短帖 | `references/content-writing.md` |
| 单个 GitHub 项目介绍 | `references/github-project-short-content.md`，成文时再用 `references/content-writing.md` |
| GitHub 项目清单 | `references/github-project-list.md`，成文时再用 `references/content-writing.md` |
| 文章或 Newsletter | `references/article-from-practice.md`，成文时再用 `references/content-writing.md` |

表达任务按用户目的选择：正面介绍或推荐、评测比较或锐评、解释教程或方法、观点或经历、清单选择或排名、新闻发布或近期变化。

### 4. 按需叠加发布事实

宣发、推广、招募、预热、发布期传播、引导试用、报名，以及“病毒式传播”“更有传播力”“爆款”等要求都继续使用已经选定的内容形式和 `references/content-writing.md`，不选择另一种写作路线。正文确实需要写入折扣、奖励、抽奖、有效时间、行动入口或合作披露时，才读取 `references/publication-requirements.md`；普通介绍、分享、推荐和产品帖没有这些事实时不加载。

用户或甲方给出的数据、定位、必写内容、叙事身份、产品揭示位置和成品数量直接构成本次写作要求。材料不足时，联网只补充能帮助选材和成文的信息；用户同时要求研究、判断或事实核查时，执行上先完成那个独立结果，再把真正适合公开表达的部分交给写作。最终回复仍完整交付用户要直接使用的文字，用户明确要求先看研究或比较报告时除外。

`references/publication-requirements.md` 负责确认会改变正文承诺或读者行动的事实，并把公开写作真正需要的部分交给普通写作流程。用户只要求方案时，交付方案后停止；用户要求具体文字时直接完成文字。单篇请求只交付一个成品，多个卖点和角度只是选材，不自动拆成多篇。

## 写作流程

### 1. 锁定必要的成品要求

只确认会改变交付结果的要求：对象、动作、内容形式与数量、语言、长度或格式、正在修改的正文、必须逐字保留的文字，以及用户明确要求沿用的说话身份、叙事关系、揭示位置、结构或参考。目标读者已经明确时一并使用；公开内容没有专业受众线索时按普通读者处理。用户指定先建立一种期待再反转时，把这条叙事关系写入本次写作要求，也就是“任务合同”。只记录实际成立的要求，不补空字段，也不加入检索判断、材料风险、事实核查清单或其它内部说明。

保留用户已经给出的内容关系。用户明确要求重点讲、围绕或突出什么时，它可以影响主线；只要求提到、带到、顺带说明或不要漏掉的内容只保证自然出现，不因此成为开头、主线或展开重点。用户没有指定重点时不在这里替用户预设角度。

同时确定本次交付载体，并在第 7 步按固定的三段格式完整展示。内容较长、结构复杂或需要继续修改时可以另存临时 Markdown，并在完整成品之后提供可点击链接。后续步骤沿用这个决定，不在最终输出时临时改判。

用户说“根据、基于、结合某份材料写”时，默认把材料作为新稿的内容来源，不把它解释成忠实摘要合同。只有用户明确要求摘要、忠实转述、逐句改写、不作推断或不得超出材料原意时，才收窄创作空间。

同一对话中的连续反馈用于确定这次成品应当呈现的正面结果，例如读者是谁、应当理解什么、全文怎样完整说完一件事；任务合同只保留这些最终要求。用户只纠正一个维度时，只改变该维度；上一对象的事实、原句和一次性结构不沿用到新对象。

### 2. 发现并准备有现实感的写作材料

读取 `references/prewriting-research.md`。普通新写、扩写或实质重组围绕当前目标读取用户材料、对象的原始页面或正式材料，以及外部来源，寻找能够扩大创作空间的场景、演示、比较、反应与自然说法。只做字词、格式、等义压缩，或用户明确只用给定材料时不联网。不联网只限制信息来源，不会把创作自动降成摘要或逐句释义。

GitHub 项目先快速打开用户给出的仓库或页面，认清对象、总定位、主要能力和演示；再查看官方材料、媒体报道、创作者演示、真实用户体验、Issue、Discussion 和社区讨论，寻找仓库之外的使用场景、结果、比较、反应和自然说法。打开实际来源阅读全文或相关上下文，不用搜索摘要代替；新来源不再增加创作可能性时停止。

搜索完成后只选择真正可能服务本篇的动作、结果、演示、比较、反应、说法和来源已经提供的必要解释，直接保留原始措辞、必要术语，以及让这些内容成立的行动者、条件、先后、原因、结果和上下文，并留下对应链接供成稿后核查。材料准备在完成取舍与原文保留后停止；面向读者的解释由第 5 步写正文时完成。官方免责声明、通用风险提醒、保守陈词、无关功能、边缘限制以及只供备查的技术信息不进入写作材料。没有找到有用的外部讨论时直接沿用现有材料，新来源开始重复已有角度时停止，不按固定来源数凑材料。

正文确实需要引导读者访问、安装、报名或执行某个动作时，再补充相应的真实入口和必要条件。用户明确要求提示词，或成品本身就是让读者把任务交给 AI 时，才准备提示词需要的任务上下文。

### 3. 按需取得作者声音

只有用户要求延续本人写法、明确提供声音样稿，或当前任务明确启用了长期声音时，才读取 `references/private-knowledge-library.md` 和 `references/personal-writing-memory.md`。私人库尚未初始化时，使用当前原话和有效样稿继续，不临时建立声音。内容查重只在用户要求查重或当前任务属于持续选题维护时运行，不作为普通写作前置步骤。

### 4. 读取专项模板与创作参考

先读取已经选定的专项模板。私人库已经配置时，再读取 `references/private-knowledge-library.md` 取得根目录，通过 `references/content-case-library.md` 和 `references/hook-library.md` 查找参考。普通短帖、产品帖和 GitHub 项目短介绍只从完整短内容中找案例；文章与 Newsletter 才读取文章案例。用户明确要求跨形态参考时再跨库查找，不能因为网络搜索打开了文章，就把文章当成短内容的默认创作参考。钩子仍按开头手法定位。私人库尚未初始化、参考库为空或没有合适参考时，使用当前材料和作者判断继续。

完整案例用于感受一篇内容怎样选择信息、组织关系和推进；钩子用于感受开头怎样自然带出后文。模型完整阅读后自由综合，不必证明每份参考影响了哪一句，也不把参考中的人物、数字、经历和事实借给当前对象。参考使写作者发现当前材料确实少了支撑核心内容的上下文时，回到第 2 步补材料；缺少的内容并不影响本篇成立时，直接继续写作。

正文包含福利、时限或行动入口时仍可读取普通案例。案例索引中的内部字段只帮助找到合适原文，不限制还能读取哪些案例，也不进入写作材料。

### 5. 形成一篇首尾完整的内容

使用 `references/content-writing.md`。完整阅读用户要求、对象材料、专项模板和已经打开的创作参考，由该文件负责确定内容中心、向普通读者解释陌生对象，并形成自然完整的开头、正文和结尾。这里不再重复它的成文规则。

字幕、访谈和会议记录先恢复完整互动与前后关系，再作为可写材料交给模型，不把时间片顺序强制变成正文顺序。行动入口、提示词、安装说明、利益与时间等内容，只在当前成品确实需要时进入相应输入。

### 6. 回读内容与表达

应用用户明确格式后，读取 `references/natural-writing.md`，按其中的顺序检查整篇是否成立、正文事实是否有材料支撑，以及表达是否自然。缺少必要材料时返回第 2 步；材料足够但整篇没有形成完整意思时返回第 5 步重写。

### 7. 展示完整写作结果

每次写作任务依次展示以下三项，标题后紧接独立代码块：

1. **任务合同**：第 1 步实际确定的要求，只写成立的内容。
2. **传给写作 AI 的内容**：原样展示本次实际使用的任务与对象材料；案例和钩子正文及路径不放进这个代码块。
3. **写作 AI 的成品内容**：展示复查后的完整正文、大纲或方案。正文含代码围栏时使用更长的外层围栏；另存临时 Markdown 时在代码块之后返回可点击链接。

本次实际使用了创作参考时，在三个代码块之后以“本次创作参考”列出这些完整案例和钩子，使用可点击的绝对文件路径，不列采用说明、评分、隐藏推理或检查日志。没有使用参考时不生成这个部分；其它事实来源只在用户要求查看依据时另行说明。

## 知识库与持久化

私人知识库独立于 Skill 源码。初始化、接入、读取或写入前先读 `references/private-knowledge-library.md`，由 `scripts/private_library.py` 返回本次唯一根目录；知识、案例、钩子和写作记忆都从这里定位，不根据当前仓库或客户端目录猜测位置。

普通解释、预览稿、短成品和临时 Markdown 只服务当前任务。用户明确要求保存、更新或沉淀时，取得私人库根目录后按 `references/knowledge-base-workflow.md` 找到唯一正式位置：同一主题更新现有文档，确实是独立主题时才新建。私人库已经配置 Marktree CLI 时，正文和索引统一经过 `scripts/marktree_integration.py` 写入；知识应放在哪里仍由本 Skill 与 `Home.md` 决定。

用户明确要求根据给定材料沉淀时，先完成材料理解，再按材料实际作用写入：需要长期保留的原始材料进入 `20-Sources`；稳定概念、机制、结论和边界合并到 `10-Knowledge` 的对应主题；材料本身是完整成品且具有创作参考价值时才进入完整案例。只有用户明确要求保存一段原始开头时，才由独立入口写入钩子库；案例导入不会派生钩子。写后重新读取正式文件并运行相应索引检查，告诉用户实际更新了什么。

发布历史和写作风格样本使用 `scripts/writing_memory.py` 的两个入口：`voice` 只读取明确允许用于学习用户写法的同类作品，`novelty` 只判断主题是否重复。用户确认、保存或更新最终作品后，按 `references/personal-writing-memory.md` 更新索引。

长期内容方向与写作风格分别保存在 `60-Systems/Writing/content-strategy.md` 和 `60-Systems/Writing/style-guide/voice.md`。持续选题只在真实内容项目的 `30-Projects/Content/<系列>/topic-portfolio.md` 保存决策状态；已发布内容复盘链接最终正文和真实反馈，不复制正文。一次结果保持为假设，只有重复证据、直接行为证据或用户确认支持的认识才进入对应的长期文件，发布表现本身不改写用户声音。

用户明确要求保存或更新完整案例时，先读取 `references/content-case-library.md`，使用 `scripts/content_case_library.py add-case` 写入完整短内容或文章案例，再运行该脚本的 `build-index` 和 `validate`。用户明确要求保存或更新钩子时，先读取 `references/hook-library.md`，使用 `scripts/hook_library.py add-hook` 写入独立钩子库，再运行该脚本的 `build-index` 和 `validate`。两个入口没有互相转换或引用的参数。

本地 SRT、VTT、带时间戳 TXT 或断行严重的字幕需要规范化时，使用 `scripts/normalize_subtitles.py`，保留原文件和真实时间位置。

个人写作记忆只提供经过确认的写作风格样本与发布历史；内容案例库只提供创作参考，两者不能替代当前对象的事实和明确承诺。普通翻译、转写、格式清洗和普通计划走对应专门流程；广告投放、销售页与落地页、邮件营销序列、品牌全案、完整营销策略和实际发布不属于本 Skill 的文字写作结果。

## 交付前检查

- 成品直接回答用户要求；资料足够时已经停止。
- 用户指定的重点、材料关系、数量、语言、格式和权限保持不变。
- 主结果已经选清；来源接入、长期保存、作者声音、发布事实和其它条件只在对应请求成立时加入。
- 当前路线已经按自己的完成标准核对，参考文件和脚本没有替主流程改变结果或继续扩展任务。
